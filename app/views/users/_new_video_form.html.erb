<%= form_for(@url) do |f| %>
  <div class="field">
    <%= f.text_field :url, class: 'form-control', placeholder: "add youtube link.." %>
    <%= f.text_field :category, class: 'form-control', placeholder: "category.." %>
  </div>
    <button style="margin-bottom: 10px;" type="submit" class="btn btn-default">+</button>
<% end %>
