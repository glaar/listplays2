<%= form_for(@url) do |f| %>
  <div class="field">
    <%= f.text_field :url, class: 'form-control', placeholder: "Add youtube link.." %>
  </div>
  <button type="submit" class="btn btn-default">+</button>
<% end %>
