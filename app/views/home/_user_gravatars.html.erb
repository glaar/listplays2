<% @users.each do |user| %>

  <% unless user == current_user%>
    <% gravatar_id = Digest::MD5::hexdigest(user.email.downcase) %>
    <% gravatar_url = "https://secure.gravatar.com/avatar/#{gravatar_id}" %>
    <%= link_to image_tag(gravatar_url), user %>
  <%end%>

<% end %>
